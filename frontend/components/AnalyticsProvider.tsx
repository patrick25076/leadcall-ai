"use client";

// PostHog is initialized via instrumentation-client.ts (Next.js 15.3+).
// This component exists only as a placeholder if we need client-side setup later.

export function AnalyticsProvider() {
  return null;
}
